apiVersion: apps/v1
kind: Deployment
metadata:
  name: mock-api-service
  namespace: nft-tracer-test
  labels:
    app: mock-api-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mock-api-service
  template:
    metadata:
      labels:
        app: mock-api-service
    spec:
      containers:
      - name: mock-api-service
        image: nft-tracer/mock-services:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8081
          name: http
        env:
        - name: SERVICE_TYPE
          value: "api"
        - name: SERVICE_PORT
          value: "8081"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8081
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: mock-api-service
  namespace: nft-tracer-test
  labels:
    app: mock-api-service
spec:
  type: ClusterIP
  ports:
  - port: 8081
    targetPort: 8081
    protocol: TCP
    name: http
  selector:
    app: mock-api-service
